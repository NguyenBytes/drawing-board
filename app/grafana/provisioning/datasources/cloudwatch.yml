apiVersion: 1

datasources:
  - name: CloudWatch
    uid: cloudwatch
    type: cloudwatch
    access: proxy
    isDefault: true
    editable: false
    version: 1
    jsonData:
      authType: default
      defaultRegion: $AWS_REGION
      assumeRoleArn: $GRAFANA_AWS_ROLE_ARN
